# Portfolio Environment Variables
# Copy this file to ~/.env and fill in your actual values

# Required for DNS automation
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Required for production deployment
JWT_SECRET=your_secure_jwt_secret_here

# Optional: External container registry
REGISTRY=your-registry.com

# Optional: Custom namespace (defaults to 'portfolio')
NAMESPACE=portfolio

# Optional: Custom domain (defaults to 'portfolio.devop.foo')  
DOMAIN=portfolio.devop.foo

# Optional: Custom email (used in SSL certs and contact info)
EMAIL=your-email@your-domain.com

# Optional: Cloudflare zone (the root domain)
CLOUDFLARE_ZONE=your-domain.com

# Contact information (used in portfolio)
REACT_APP_CONTACT_EMAIL=your-email@your-domain.com

# NPM Reverse Proxy Server Configuration
PROXY_HOST=192.168.1.111
PROXY_EMAIL=your-npm-email@example.com
PROXY_PASSWORD=your-npm-password

# Instructions:
# 1. Get your Cloudflare API token from: https://dash.cloudflare.com/profile/api-tokens
# 2. Generate a secure JWT secret: openssl rand -hex 32
# 3. Export these variables or add them to your ~/.env file
# 4. Source your .env file: source ~/.env